# Factorio API credentials
# Get your token from https://www.factorio.com/profile
FACTORIO_USERNAME=your_username
FACTORIO_TOKEN=your_token

# SurrealDB configuration
# Storage options:
#   mem://              - In-memory (volatile, requires kv-mem feature)
#   surrealkv://./data  - File-based (persistent, requires kv-surrealkv feature)
#   ws://localhost:8000 - Remote server (requires protocol-ws feature)
SURREAL_URL=surrealkv://./data
SURREAL_NS=factorio
SURREAL_DB=tracker

# SurrealDB credentials (only needed for remote server connections)
# SURREAL_USER=root
# SURREAL_PASS=root
